interface:
  display_name: "Wechat Super Power"
  short_description: "公众号选题、知识库、写作与图文配图"
  default_prompt: "Use $wechat-super-power to run the WeChat article workflow: research a topic, build a knowledge base, analyze hotspots, draft the article, and generate cover/inline image plans or images when requested."

policy:
  allow_implicit_invocation: true
